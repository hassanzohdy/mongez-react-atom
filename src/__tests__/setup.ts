// Vitest setup for the react-atom package.
//
// Kept intentionally empty; the happy-dom environment is configured via
// vitest.config.ts and that's all the React layer currently needs. If we
// later adopt matchers like `@testing-library/jest-dom`, import them here.
export {};
